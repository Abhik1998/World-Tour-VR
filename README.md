# World-Tour-VR
My first application built using React360. It's a simple combination of 3D frames.

In order to run the application follow the steps:-
```
npm install -g react-360-cli
```
Download the repo and within the directory, run the following command:-

```
npm i
npm start
```

Thanks ....
